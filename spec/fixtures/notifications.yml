events:
  signed_up:
    push:
      title: Hello title
      body: Hello body!
    email:
      subject: Hello subject
      body: Hello body

rspec:
  test_notification:
    push:
      title: Test, %{pass_variable}
      body: Test, %{pass_variable}
    email:
      subject: Test, %{pass_variable}
      body: Test, %{pass_variable}
  test_notification_with_mailer_class:
    push:
      title: Test, %{pass_variable}
      body: Test, %{pass_variable}
    email:
      subject: Test, %{pass_variable}
      body: Test, %{pass_variable}
      mailer_class: RspecMailer
  test_notification_with_mailer:
    push:
      title: Test, %{pass_variable}
      body: Test, %{pass_variable}
    email:
      subject: Test, %{pass_variable}
      body: Test, %{pass_variable}
      mailer_class: RspecMailer
      mailer_method: welcome
  test_notification_with_mailer_method:
    push:
      title: Test, %{pass_variable}
      body: Test, %{pass_variable}
    email:
      subject: Test, %{pass_variable}
      body: Test, %{pass_variable}
      mailer_method: welcome


