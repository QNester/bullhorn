events:
  signed_up:
    push:
      title: Hello title
      body: Hello body!
    email:
      subject: Hello subject
      body: Hello body

rspec:
  test_notification:
    push:
      title: Test, %{pass_variable}
      body: Test, %{pass_variable}
    email:
      subject: Test, %{pass_variable}
      body: Test, %{pass_variable}

