require 'spec_helper'

RSpec.describe HeyYou::Channels::Push do
  describe 'send!' do
    context 'all credentials presents' do
      pending 'send msg via fcm'
    end

    context 'not all credentials presents' do
      pending 'not send msg via fcm'
    end
  end
end