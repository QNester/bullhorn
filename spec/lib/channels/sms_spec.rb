require 'spec_helper'

RSpec.describe Horn::Channels::Sms do
  describe 'send!' do
    context 'all credentials presents' do
      pending 'send message to twilio'
    end

    context 'not all credentials presents' do
      pending 'not call twilio'
    end
  end
end